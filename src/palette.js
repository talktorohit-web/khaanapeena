// Categorical chart palette — validated CVD-safe on a light surface, fixed order
// (never cycled beyond its length). Kept in its own module so charts.jsx exports
// only components and Vite Fast Refresh stays intact.
export const CAT = ['#f06008', '#2563eb', '#16a34a', '#9333ea']
